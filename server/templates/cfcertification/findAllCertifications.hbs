{{#if data.length}}
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>{{t 'id'}}</th>
        <th>{{t 'name'}}</th>
        <th>Usuário</th>
        <th>{{t 'createdAt'}}</th>
        <th>{{t 'updatedAt'}}</th>
        <th>{{t 'actions'}}</th>
      </tr>
    </thead>
    <tbody>
    {{#each data}}
      <tr>
        <td>{{id}}</td>
        <td>{{name}}</td>
        <td>{{user.displayName}}</td>
        <td>{{we-date createdAt}}</td>
        <td>{{we-date updatedAt}}</td>
        <td>
          <a
            class="btn btn-sm btn-default"
            href="/user/{{user.id}}/certification/{{id}}/download.pdf"
            download="certification_u{{user.id}}_{{identifier}}.pdf"
          ><span class="glyphicon glyphicon-cloud-download" aria-hidden="true"></span> Download</a>
        </td>
      </tr>
    {{/each}}
    </tbody>
    <tfoot>
      <tr>
        <td colspan="6">
          <div class="paginate-sumary">
            {{t 'paginate.sumary' recordsLength=data.length count=metadata.count}}
          </div>

          {{paginate count=metadata.count limit=query.limit currentPage=query.page req=req}}
        </td>
      </tr>
    </tfoot>
  </table>
{{else}}
  <p>O evento / seminário não possuí certificados gerados.</p>
  <br>
  <h4>Para gerar certificados siga os passos abaixo:</h4>
  <ol>
    <li>Configure o template de certificado para cada tipo de inscrição: <a href="/event/{{event.id}}/admin/cfregistrationtype">Link</a></li>
    <li>Marque os usuários como presentes no evento: <a href="/event/{{event.id}}/admin/cfregistration">Link</a></li>
  </ol>
{{/if}}